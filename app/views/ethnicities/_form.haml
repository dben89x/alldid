.col-md-12
	%br
	%br
	= form_for(ethnicity) do |f|
		- if ethnicity.errors.any?
			#error_explanation
				%h2
					= pluralize(ethnicity.errors.count, "error")
					prohibited this ethnicity from being saved:
				%ul
					- ethnicity.errors.full_messages.each do |message|
						%li= message
		.col-md-12
			.ethnicity-field
				="Name"
				%br
				= f.text_field :name, autofocus: true, class: 'input ethnicity-input'
				%br
				%br
		.actions
			= f.submit class: 'brand-btn med-btn'
	= link_to 'Back', ethnicities_path
